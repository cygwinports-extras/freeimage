--- origsrc/FreeImage/Makefile.cygwin	2005-11-26 02:04:04.000000000 -0600
+++ src/FreeImage/Makefile.cygwin	2013-12-31 17:53:04.153671700 -0600
@@ -23,7 +23,7 @@ CXXFLAGS = $(COMPILERFLAGS)  -Wno-ctor-d
 
 TARGET  = freeimage
 STATICLIB = lib$(TARGET).a
-SHAREDLIB = cyg$(TARGET)-$(VER_MAJOR).$(VER_MINOR).dll
+SHAREDLIB = cyg$(TARGET)-$(VER_MAJOR).dll
 LIBNAME = lib$(TARGET).dll.a
 HEADER = Source/FreeImage.h
 
@@ -52,8 +52,8 @@ $(STATICLIB): $(MODULES)
 $(STATICLIB): $(MODULES)
 	$(AR) r $@ $(MODULES)
 
-$(SHAREDLIB): $(MODULES)
-	$(CC) -s -shared -o $@ \
+$(SHAREDLIB): $(STATICLIB)
+	$(CC) -shared -o $@ \
 	    -Wl,--out-implib=$(LIBNAME) \
 	    -Wl,--export-all-symbols \
 	    -Wl,--enable-auto-import \
--- origsrc/FreeImage/Makefile.fip	2007-08-25 16:57:54.000000000 -0500
+++ src/FreeImage/Makefile.fip	2013-12-31 17:55:54.171432500 -0600
@@ -24,8 +24,8 @@ CXXFLAGS = $(COMPILERFLAGS) -Wno-ctor-dt
 
 TARGET  = freeimageplus
 STATICLIB = lib$(TARGET).a
-SHAREDLIB = lib$(TARGET)-$(VER_MAJOR).$(VER_MINOR).so
-LIBNAME	= lib$(TARGET).so
+SHAREDLIB = cyg$(TARGET)-$(VER_MAJOR).dll
+LIBNAME	= lib$(TARGET).dll.a
 VERLIBNAME = $(LIBNAME).$(VER_MAJOR)
 HEADER = Source/FreeImage.h
 HEADERFIP = Wrapper/FreeImagePlus/FreeImagePlus.h
@@ -37,7 +37,8 @@ all: dist
 
 dist: FreeImage
 	cp *.a Dist
-	cp *.so Dist
+	cp *.dll Dist
+	cp *.dll.a Dist
 	cp Source/FreeImage.h Dist
 	cp Wrapper/FreeImagePlus/FreeImagePlus.h Dist
 
@@ -56,7 +57,7 @@ $(STATICLIB): $(MODULES)
 	$(AR) r $@ $(MODULES)
 
 $(SHAREDLIB): $(MODULES)
-	$(CC) -s -shared -Wl,-soname,$(VERLIBNAME) -o $@ $(MODULES) $(LIBRARIES)
+	$(CC) -shared -Wl,--out-implib,$(LIBNAME) -o $@ $(MODULES) $(LIBRARIES)
 
 install:
 	install -m 644 -o root -g root $(HEADER) $(INCDIR)
